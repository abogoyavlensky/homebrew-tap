# homebrew-tap

Homebrew tap for abogoayvlensky's projects.

## [lgx](https://github.com/abogoyavlensky/lgx)

### Install

```sh
brew install abogoyavlensky/tap/lgx
```

Works on macOS and Linux, Intel and ARM. The command taps this
repository automatically.

lgx also needs the let-go compiler (`lg` >= 1.10.0) on `PATH`. On macOS:

```sh
brew tap nooga/let-go https://github.com/nooga/let-go && brew install let-go
```

On Linux and for other options, see the
[lgx requirements](https://github.com/abogoyavlensky/lgx#requirements).

### Upgrade

```sh
brew upgrade lgx
```

### How this tap is updated

`Formula/lgx.rb` is generated by
[`scripts/generate-formula.sh`](https://github.com/abogoyavlensky/lgx/blob/master/scripts/generate-formula.sh)
in the lgx repository. Release CI regenerates and pushes it on every
release - don't edit it by hand.

## [wtr](https://github.com/abogoyavlensky/wtr)

### Install

```sh
brew install abogoyavlensky/tap/wtr
```

Works on macOS and Linux, Intel and ARM. The command taps this
repository automatically.

### Upgrade

```sh
brew upgrade wtr
```

### How this tap is updated

`Formula/wtr.rb` is generated by
[`scripts/generate-formula.sh`](https://github.com/abogoyavlensky/wtr/blob/master/scripts/generate-formula.sh)
in the wtr repository. Release CI regenerates and pushes it on every
release - don't edit it by hand.

## [clj-pulse](https://github.com/abogoyavlensky/clj-pulse)

### Install

```sh
brew install abogoyavlensky/tap/clj-pulse
```

Works on macOS and Linux, Intel and ARM. The command taps this
repository automatically.

### Upgrade

```sh
brew upgrade clj-pulse
```

### How this tap is updated

`Formula/clj-pulse.rb` is generated by
[`scripts/generate-formula.sh`](https://github.com/abogoyavlensky/clj-pulse/blob/master/scripts/generate-formula.sh)
in the clj-pulse repository. Release CI regenerates and pushes it on every
release - don't edit it by hand.
